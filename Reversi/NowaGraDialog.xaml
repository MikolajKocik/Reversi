<Window x:Class="Reversi.NowaGraDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Nowa gra - ustawienia" SizeToContent="WidthAndHeight" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0">Szerokosc:</Label>
        <TextBox x:Name="WidthTextBox" Grid.Row="0" Grid.Column="1" MinWidth="50" Text="8"/>

        <Label Grid.Row="1" Grid.Column="0">Wysokosc:</Label>
        <TextBox x:Name="HeightTextBox" Grid.Row="1" Grid.Column="1" MinWidth="50" Text="8"/>

        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button IsDefault="True" Click="OkButton_Click" MinWidth="60" Margin="0,0,10,0">OK</Button>
            <Button IsCancel="True" MinWidth="60">Anuluj</Button>
        </StackPanel>
    </Grid>
</Window>
